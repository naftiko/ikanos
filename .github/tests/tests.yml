tests:
  - name: step1-get-ship
    file: step-1-shipyard-mock.yml
    port: 3001
    tool: get-ship
    args:
      imo_number: "IMO-9321483"          
    validations:
      - jq: 'has("imo")'
      - jq: 'has("name")'
      - jq: 'has("type")'
      - jq: 'has("flag")'
      - jq: 'has("status")'
      - jq: '.imo == "IMO-9321483"'

  - name: step2-list-ships
    file: step-2-shipyard-wiring.yml
    port: 3001
    tool: list-ships
    args: {}
    validations:
      - jq: 'type == "array"'
      - jq: 'length > 0'
      - jq: '.[0] | has("imo")'
      - jq: '.[0] | has("name")'
      - jq: '.[0] | has("type")'
      - jq: '.[0] | has("flag")'
      - jq: '.[0] | has("status")'

  - name: step2-list-ships-filter
    file: step-2-shipyard-wiring.yml
    port: 3001
    tool: list-ships
    args:
      status: active
    validations:
      - jq: 'type == "array"'
      - jq: 'length > 0'
      - jq: '.[0] | has("imo")'
      - jq: 'all(.[]; .status == "active")'   

  - name: step2-get-ship
    file: step-2-shipyard-wiring.yml
    port: 3001
    tool: get-ship
    args:
      imo: "IMO-9321483"
    validations:
      - jq: 'has("imo")'
      - jq: 'has("name")'
      - jq: 'has("type")'
      - jq: 'has("flag")'
      - jq: 'has("status")'

  - name: step3-list-ships
    file: step-3-shipyard-auth.yml
    port: 3001
    tool: list-ships
    args: {}
    validations:
      - jq: 'type == "array"'
      - jq: 'length > 0'
      - jq: '.[0] | has("imo")'             
      - jq: '.[0] | has("name")'
      - jq: '.[0] | has("status")'

  - name: step3-get-ship
    file: step-3-shipyard-auth.yml
    port: 3001
    tool: get-ship
    args:
      imo: "IMO-9321483"
    validations:
      - jq: 'type == "object"'
      - jq: 'has("imo")'
      - jq: 'has("name")'
      - jq: 'has("type")'
      - jq: 'has("flag")'
      - jq: 'has("status")'

  - name: step3-get-ship-no-token
    file: step-3-shipyard-auth.yml
    port: 3001
    tool: get-ship
    token: none
    expectStatus: 401
    args:
      imo: "IMO-9321483"
    validations: []

  - name: step3-get-ship-invalid-token
    file: step-3-shipyard-auth.yml
    port: 3001
    tool: get-ship
    token: not-a-real-token
    expectStatus: 401
    args:
      imo: "IMO-9321483"
    validations: []

  - name: step4-get-ship-shaped
    file: step-4-shipyard-output-shaping.yml
    port: 3001
    tool: get-ship
    args:
      imo: "IMO-9321483"
    validations:
      - jq: 'has("imo")'
      - jq: 'has("name")'
      - jq: 'has("type")'
      - jq: 'has("flag")'
      - jq: 'has("status")'
      - jq: 'has("specs")'
      - jq: '.specs | has("yearBuilt")'
      - jq: '.specs | has("tonnage")'
      - jq: '.specs | has("length")'
      - jq: '.specs.yearBuilt | type == "number"'   
      - jq: '.specs.tonnage  | type == "number"'
      - jq: '.specs.length   | type == "number"'

  - name: step5-list-ships
    file: step-5-shipyard-multi-source.yml
    port: 3001
    tool: list-ships
    args: {}
    validations:
      - jq: 'type == "array"'
      - jq: 'length > 0'
      - jq: '.[0] | has("imo")'
      - jq: '.[0] | has("name")'
      - jq: '.[0] | has("status")'

  - name: step5-list-legacy-vessels
    file: step-5-shipyard-multi-source.yml
    port: 3001
    tool: list-legacy-vessels
    args: {}
    validations:
      - jq: 'type == "array"'
      - jq: 'length > 0'
      - jq: '.[0] | has("vesselCode")'
      - jq: '.[0] | has("name")'
      - jq: '.[0] | has("type")'
      - jq: '.[0] | has("flag")'
      - jq: '.[0] | has("status")'

  - name: step6-list-ships
    file: step-6-shipyard-write-operations.yml
    port: 3001
    tool: list-ships
    args: {}
    validations:
      - jq: 'type == "array"'
      - jq: 'length > 0'
      - jq: '.[0] | has("imo")'

  - name: step6-list-legacy-vessels
    file: step-6-shipyard-write-operations.yml
    port: 3001
    tool: list-legacy-vessels
    args: {}
    validations:
      - jq: 'type == "array"'
      - jq: 'length > 0'
      - jq: '.[0] | has("vesselCode")'
      - jq: '.[0] | has("name")'
      - jq: '.[0] | has("type")'
      - jq: '.[0] | has("flag")'
      - jq: '.[0] | has("status")'

  - name: step6-create-voyage
    file: step-6-shipyard-write-operations.yml
    port: 3001
    tool: create-voyage
    args:
      shipImo: "IMO-9321483"
      departurePort: "Oslo"
      arrivalPort: "Singapore"
      departureDate: "2026-04-10"
      arrivalDate: "2026-05-02"
      crewIds: ["CREW-001", "CREW-003"]
      cargoIds: ["CARGO-2024-0451"]
    validations:
      - jq: 'has("voyageId")'
      - jq: 'has("shipImo")'
      - jq: 'has("route")'
      - jq: '.route | has("from")'
      - jq: '.route | has("to")'
      - jq: 'has("dates")'
      - jq: '.dates | has("departure")'
      - jq: '.dates | has("arrival")'
      - jq: 'has("status")'
      - jq: 'has("crewIds")'
      - jq: '.crewIds | length > 0'
      - jq: 'has("cargoIds")'             

  - name: step7-get-ship-with-crew
    file: step-7-shipyard-orchestrated-lookup.yml
    port: 3001
    tool: get-ship-with-crew
    args:
      imo: "IMO-9321483"
    validations:
      - jq: 'has("imo")'
      - jq: 'has("name")'
      - jq: 'has("type")'                 
      - jq: 'has("flag")'
      - jq: 'has("status")'
      - jq: 'has("crew")'
      - jq: '.crew | type == "array"'
      - jq: '.crew | length > 0'
      - jq: '.crew[0] | has("fullName")'
      - jq: '.crew[0] | has("role")'

  - name: step7-create-voyage
    file: step-7-shipyard-orchestrated-lookup.yml
    port: 3001
    tool: create-voyage
    args:
      shipImo: "IMO-9321483"
      departurePort: "Oslo"
      arrivalPort: "Singapore"
      departureDate: "2026-04-10"
      arrivalDate: "2026-05-02"
      crewIds: ["CREW-001", "CREW-003"]
      cargoIds: ["CARGO-2024-0451"]
    validations:
      - jq: 'has("voyageId")'
      - jq: 'has("shipImo")'             
      - jq: 'has("route")'
      - jq: '.route | has("from")'       
      - jq: '.route | has("to")'         
      - jq: 'has("dates")'               
      - jq: '.dates | has("departure")'  
      - jq: '.dates | has("arrival")'    
      - jq: 'has("status")'
      - jq: 'has("crewIds")'             
      - jq: '.crewIds | length > 0'      
  - name: step8-list-ships
    file: step-8-shipyard-skill-groups.yml
    port: 3001
    tool: list-ships
    args: {}
    validations:
      - jq: 'type == "array"'
      - jq: 'length > 0'
      - jq: '.[0] | has("imo")'          

  - name: step8-get-ship-with-crew
    file: step-8-shipyard-skill-groups.yml
    port: 3001
    tool: get-ship-with-crew
    args:
      imo: "IMO-9321483"
    validations:
      - jq: 'has("imo")'
      - jq: 'has("name")'
      - jq: 'has("type")'                
      - jq: 'has("flag")'
      - jq: 'has("status")'
      - jq: 'has("crew")'
      - jq: '.crew | type == "array"'
      - jq: '.crew | length > 0'
      - jq: '.crew[0] | has("fullName")'
      - jq: '.crew[0] | has("role")'

  - name: step8-create-voyage
    file: step-8-shipyard-skill-groups.yml
    port: 3001
    tool: create-voyage
    args:
      shipImo: "IMO-9321483"
      departurePort: "Oslo"
      arrivalPort: "Singapore"
      departureDate: "2026-04-10"
      arrivalDate: "2026-05-02"
      crewIds: ["CREW-001", "CREW-003"]
      cargoIds: ["CARGO-2024-0451"]
    validations:
      - jq: 'has("voyageId")'
      - jq: 'has("shipImo")'
      - jq: 'has("route")'
      - jq: '.route | has("from")'
      - jq: '.route | has("to")'
      - jq: 'has("dates")'
      - jq: '.dates | has("departure")'
      - jq: '.dates | has("arrival")'
      - jq: 'has("status")'
      - jq: 'has("crewIds")'
      - jq: '.crewIds | length > 0'

  - name: step8-skill-fleet-ops
    type: rest
    file: step-8-shipyard-skill-groups.yml
    port: 3002
    method: GET
    path: /skills/fleet-ops
    validations:
      - jq: 'has("name")'
      - jq: '.name == "fleet-ops"'
      - jq: 'has("tools")'
      - jq: '.tools | length > 0'
      - jq: '.tools[] | has("name")'

  - name: step9-list-ships
    file: step-9-shipyard-aggregates.yml
    port: 3001
    tool: list-ships
    args: {}
    validations:
      - jq: 'type == "array"'
      - jq: 'length > 0'
      - jq: '.[0] | has("imo")'          

  - name: step9-get-ship-with-crew
    file: step-9-shipyard-aggregates.yml
    port: 3001
    tool: get-ship-with-crew
    args:
      imo: "IMO-9321483"
    validations:
      - jq: 'has("imo")'
      - jq: 'has("name")'
      - jq: 'has("type")'                
      - jq: 'has("flag")'
      - jq: 'has("status")'
      - jq: 'has("crew")'
      - jq: '.crew | type == "array"'
      - jq: '.crew | length > 0'
      - jq: '.crew[0] | has("fullName")'
      - jq: '.crew[0] | has("role")'

  - name: step9-create-voyage
    file: step-9-shipyard-aggregates.yml
    port: 3001
    tool: create-voyage
    args:
      shipImo: "IMO-9321483"
      departurePort: "Oslo"
      arrivalPort: "Singapore"
      departureDate: "2026-04-10"
      arrivalDate: "2026-05-02"
      crewIds: ["CREW-001", "CREW-003"]
      cargoIds: ["CARGO-2024-0451"]
    validations:
      - jq: 'has("voyageId")'
      - jq: 'has("shipImo")'
      - jq: 'has("route")'
      - jq: '.route | has("from")'
      - jq: '.route | has("to")'
      - jq: 'has("dates")'
      - jq: '.dates | has("departure")'
      - jq: '.dates | has("arrival")'
      - jq: 'has("status")'
      - jq: 'has("crewIds")'
      - jq: '.crewIds | length > 0'

  - name: step10-mcp-list-ships
    file: step-10-shipyard-rest-adapter.yml
    port: 3001
    tool: list-ships
    args: {}
    validations:
      - jq: 'type == "array"'
      - jq: 'length > 0'
      - jq: '.[0] | has("imo")'          

  - name: step10-rest-list-ships
    type: rest
    file: step-10-shipyard-rest-adapter.yml
    port: 3003
    method: GET
    path: /ships
    validations:
      - jq: 'type == "array"'
      - jq: 'length > 0'
      - jq: '.[0] | has("imo")'
      - jq: '.[0] | has("name")'
      - jq: '.[0] | has("status")'

  - name: step10-rest-get-ship
    type: rest
    file: step-10-shipyard-rest-adapter.yml
    port: 3003
    method: GET
    path: /ships/IMO-9321483
    validations:
      - jq: 'has("imo")'
      - jq: 'has("name")'
      - jq: 'has("type")'                
      - jq: 'has("flag")'                
      - jq: 'has("status")'              
      - jq: 'has("specs")'
      - jq: '.specs | has("yearBuilt")'
      - jq: '.specs | has("tonnage")'
      - jq: '.specs | has("length")'

  - name: step10-rest-get-ship-with-crew
    type: rest
    file: step-10-shipyard-rest-adapter.yml
    port: 3003
    method: GET
    path: /ships/IMO-9321483/crew
    validations:
      - jq: 'has("imo")'
      - jq: 'has("name")'
      - jq: 'has("type")'                
      - jq: 'has("flag")'
      - jq: 'has("status")'
      - jq: 'has("crew")'
      - jq: '.crew | type == "array"'
      - jq: '.crew | length > 0'
      - jq: '.crew[0] | has("fullName")'
      - jq: '.crew[0] | has("role")'

  - name: step10-rest-list-legacy-vessels
    type: rest
    file: step-10-shipyard-rest-adapter.yml
    port: 3003
    method: GET
    path: /legacy/vessels
    validations:
      - jq: 'type == "array"'
      - jq: 'length > 0'
      - jq: '.[0] | has("vesselCode")'
      - jq: '.[0] | has("name")'         
      - jq: '.[0] | has("type")'         
      - jq: '.[0] | has("flag")'         
      - jq: '.[0] | has("status")'       

  - name: step10-rest-create-voyage
    type: rest
    file: step-10-shipyard-rest-adapter.yml
    port: 3003
    method: POST
    path: /voyages
    body:
      shipImo: "IMO-9321483"
      departurePort: "Oslo"
      arrivalPort: "Singapore"
      departureDate: "2026-04-10"
      arrivalDate: "2026-05-02"
      crewIds: ["CREW-001", "CREW-003"]
      cargoIds: ["CARGO-2024-0451"]
    validations:
      - jq: 'has("voyageId")'
      - jq: 'has("shipImo")'             
      - jq: 'has("route")'
      - jq: '.route | has("from")'       
      - jq: '.route | has("to")'         
      - jq: 'has("dates")'               
      - jq: '.dates | has("departure")'  
      - jq: '.dates | has("arrival")'    
      - jq: 'has("status")'
      - jq: 'has("crewIds")'             
      - jq: '.crewIds | length > 0'      

  - name: step11-mcp-list-ships
    file: step-11-shipyard-fleet-manifest.yml
    port: 3001
    tool: list-ships
    args: {}
    validations:
      - jq: 'type == "array"'
      - jq: 'length > 0'
      - jq: '.[0] | has("imo")'

  - name: step11-mcp-get-voyage-manifest
    file: step-11-shipyard-fleet-manifest.yml
    port: 3001
    tool: get-voyage-manifest
    args:
      voyageId: "VOY-2026-042"
    validations:
      - jq: 'has("voyageId")'
      - jq: 'has("status")'
      - jq: 'has("route")'
      - jq: '.route | has("from")'
      - jq: '.route | has("to")'
      - jq: 'has("ship")'
      - jq: '.ship | has("name")'
      - jq: '.ship | has("type")'
      - jq: '.ship | has("flag")'
      - jq: 'has("crew")'
      - jq: '.crew | type == "array"'
      - jq: '.crew | length > 0'
      - jq: '.crew[0] | has("fullName")'
      - jq: '.crew[0] | has("role")'
      - jq: 'has("cargo")'
      - jq: '.cargo | type == "array"'
      - jq: '.cargo | length > 0'
      - jq: '.cargo[0] | has("description")'
      - jq: '.cargo[0] | has("weight")'
      - jq: '.cargo[0] | has("hazardous")'

  - name: step11-rest-list-ships
    type: rest
    file: step-11-shipyard-fleet-manifest.yml
    port: 3002
    method: GET
    path: /ships
    validations:
      - jq: 'type == "array"'
      - jq: 'length > 0'
      - jq: '.[0] | has("imo")'

  - name: step11-rest-get-ship
    type: rest
    file: step-11-shipyard-fleet-manifest.yml
    port: 3002
    method: GET
    path: /ships/IMO-9321483
    validations:
      - jq: 'has("imo")'
      - jq: 'has("name")'
      - jq: 'has("type")'
      - jq: 'has("flag")'
      - jq: 'has("status")'
      - jq: 'has("specs")'
      - jq: '.specs | has("yearBuilt")'
      - jq: '.specs | has("tonnage")'
      - jq: '.specs | has("length")'

  - name: step11-rest-create-voyage
    type: rest
    file: step-11-shipyard-fleet-manifest.yml
    port: 3002
    method: POST
    path: /voyages
    body:
      shipImo: "IMO-9321483"
      departurePort: "Oslo"
      arrivalPort: "Singapore"
      departureDate: "2026-04-10"
      arrivalDate: "2026-05-02"
      crewIds: ["CREW-001", "CREW-003"]
      cargoIds: ["CARGO-2024-0451"]
    validations:
      - jq: 'has("voyageId")'
      - jq: 'has("route")'
      - jq: '.route | has("from")'
      - jq: '.route | has("to")'
      - jq: 'has("status")'

  - name: step11-rest-voyage-manifest
    type: rest
    file: step-11-shipyard-fleet-manifest.yml
    port: 3002
    method: GET
    path: /voyages/VOY-2026-042/manifest
    validations:
      - jq: 'has("voyageId")'
      - jq: 'has("status")'
      - jq: 'has("departurePort")'
      - jq: 'has("arrivalPort")'
      - jq: 'has("shipName")'
      - jq: 'has("shipType")'
      - jq: 'has("shipFlag")'
      - jq: 'has("crew")'
      - jq: '.crew | type == "array"'
      - jq: '.crew | length > 0'
      - jq: '.crew[0] | has("fullName")'
      - jq: '.crew[0] | has("role")'
      - jq: 'has("cargo")'
      - jq: '.cargo | type == "array"'
      - jq: '.cargo | length > 0'
      - jq: '.cargo[0] | has("description")'
      - jq: '.cargo[0] | has("weight")'
      - jq: '.cargo[0] | has("hazardous")'